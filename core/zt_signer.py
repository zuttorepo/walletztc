from bitcoin import *

def build_and_sign_tx(from_addr, to_addr, amount, privkey):
    # Dummy TX (harus diganti TX builder real dari UTXO)
    tx = f"Signed TX from {from_addr} to {to_addr} for {amount} BTC"
    return tx
